export interface UserListObj {
  id: number;
  name: string;
  email: string;
  contact: string;
  image: string;
  active: string;
  regData: string;
  address?: string;
  appleId?: string;
  kakaoId?: string;
  notes?: string;
  birthday?: string;
}
